# Folder Structure

Moved to [PROJECT_STRUCTURE.md](../PROJECT_STRUCTURE.md) at the repository root, as part of the engineering governance package. That is now the single source of truth for the monorepo layout — kept at root alongside `CONTRIBUTING.md` and the other governance documents so it's visible without digging into `docs/`.

This file is kept only so existing links (from [docs/architecture/](architecture/) and [docs/adr/](adr/)) don't break; do not add new content here.
