# 2. Sprint 0 Technical Debt Report

Severity: **Critical** (blocks Sprint 1 / active risk) · **High** (fix soon, real exposure) · **Medium** (real gap, not urgent) · **Low** (cosmetic/minor).
Timeline: **Today** (before Sprint 1 starts) · **Sprint 5** · **Sprint 10** · **Version 2**.

## Findings

| # | Item | Area | Severity | Timeline | Why |
|---|---|---|---|---|---|
| 1 | `drizzle-orm@0.36.4` has a known High (7.5) vulnerability (GHSA-gpj5-g38j-94v9), fixed in `0.45.2` | Dependency Graph | High | Sprint 5 (dedicated story, not a drive-by) | Real, runtime, production dependency (not dev tooling) — but a 9-minor-version jump with likely breaking `migrate()`/query-builder API changes, including the exact `migrate()` options this sprint twice found real bugs in. Bumping it inside an unrelated PR risks reintroducing those bugs with no dedicated regression cycle. Needs its own story with full re-verification of every `persistence-postgres/*` package and `tools/sprint0-demo`. |
| 2 | `esbuild@0.18.20`/`0.19.12` (GHSA-67mh-4wv8-2f99, Medium 5.3) pulled in transitively via `drizzle-kit`'s own tooling chain | Dependency Graph | Low | Sprint 10 | Verified the actual exposure: this esbuild is only ever invoked by `drizzle-kit`'s one-shot CLI (`db:generate`/`db:migrate`), never as a long-running dev server exposed to a network — the advisory's actual mechanism (a dev server accepting cross-origin requests) doesn't apply to how this repo uses it. Real CVE, negligible actual exposure here. |
| 3 | `postcss@8.4.31` (GHSA-qx2v-qp2m-jg93, Medium 6.1) bundled inside `next@15.5.20` itself | Dependency Graph | Low | Version 2 | Next vendors its own postcss version; forcing a `pnpm` override on a framework's internal build tooling risks subtle breakage in its CSS pipeline with no clear benefit, since this processes only this repo's own trusted CSS at build time, never untrusted input. Correct fix is waiting for Next's own dependency bump, not overriding it ourselves. |
| 4 | `CapabilityResolverPort` (`packages/ports`) has zero adapter implementations | Ports / Capability Registry | Medium | Sprint 1 (tied to SAF-24/32) | `tools/sprint0-demo` and `apps/orchestrator`'s plugin-loader both compose the underlying domain functions (`resolveCapabilityProvider`) directly rather than through a real port adapter, since nothing yet needs runtime-swappable resolution (one plugin, one provider). Becomes load-bearing once a second capability provider type (agent, human, external-service) exists. |
| 5 | `plugin-import-boundary` (SAF-19) is narrower than `12-risks-and-technical-debt.md`'s original wording — permits named plugin imports from three composition roots rather than requiring a dynamic, manifest-driven loader | Plugin SDK / Architecture | Medium | Sprint 1/2 (SAF-25) | Documented explicitly in both the risk register and the SAF-19/21 backlog entries at the time it was found — not a silent drift. Correct once plugin process/container isolation (SAF-25) makes a real loader necessary anyway; building one now, for one first-party plugin, would be speculative. |
| 6 | `Workspace` (context-project), `Risk` (context-governance), `Capability`/`CapabilityProvider` (context-capability-registry) have no `persistence-postgres/*` module — domain aggregates only, never persisted | Bounded Contexts / Repository Structure | Medium | Sprint 1–5, per-context, as each becomes load-bearing | Consistent with `TenantRepository`/`AuditEventRepository` being the only two contexts SAF-14 built persistence for — deliberately not all ten, since nothing yet calls the others through a real use case. |
| 7 | `SecretsVaultPort` (`packages/ports`) has zero adapters — not even the documented dev one (`packages/config`, referenced in `08-authentication-and-rbac.md`, was never built) | Ports / Configuration | Medium | Sprint 1 | Every app currently reads `process.env` directly in its own `main.ts`/`build-dependencies.ts` with dev-only defaults (e.g. `dev-only-client-secret`) — the correct composition-root pattern, but with no port-level abstraction backing it. Fine for Sprint 0 dev; a real gap the moment secret rotation or a non-`.env` backend is needed. |
| 8 | No production migration-runner exists — `migrate()` is only ever called from each `persistence-postgres/*` package's own test `beforeAll`, or from `tools/sprint0-demo`'s script | Configuration / CI-CD | Medium | Sprint 1 | A real deploy pipeline needs a dedicated migrate step, not migrations riding along inside test setup or a demo script. No such pipeline exists yet (`deploy-dev` is a stated placeholder), so this hasn't blocked anything real — but it's the next thing that will, the moment `deploy-dev` becomes real. |
| 9 | GitHub Actions has never actually executed `ci.yml` — every stage was verified by running its underlying commands locally, never by a real GitHub-hosted runner | CI/CD | High | Today (once push is authorized) | This is the single largest gap between "verified" and "proven" in this whole audit: `actionlint` confirms the YAML is syntactically and semantically valid, and every command it runs has been independently verified to work — but the workflow file itself has never been executed end to end by GitHub's own infrastructure. Push is deliberately deferred (no git credentials in this environment) — not a defect, but a real, outstanding verification gap that should close as soon as push is authorized, before treating CI as load-bearing. |
| 10 | The `adapter-llm-anthropic`/`adapter-mcp-stdio` package renames flagged by the mid-sprint architecture inventory review (vendor-named but fully generic adapters) were never done | Code Organization / Package Structure | Low | Sprint 5 | Cosmetic — the packages work correctly and are architecturally sound (generic resilience wrapper + a swappable concrete adapter); only the package *name* implies more vendor lock-in than actually exists. Flagged in the mid-sprint review, carried forward unresolved through the rest of Sprint 0, still open. |
| 11 | The `Notification` bounded context's fate is unresolved — described with real aggregates in `02-domain-model.md` and referenced as an event consumer in `06-event-model.md`, but never given a package, and `PROJECT_STRUCTURE.md`'s canonical tree never included it | Bounded Contexts / Documentation | Medium | Sprint 1 | Found at SAF-8, flagged, carried forward unresolved through the rest of Sprint 0 (confirmed still true by this audit — no later story addressed it). Not a functional gap (nothing currently needs to send a notification), but a real documentation/architecture inconsistency: two docs partially describe a thing that doesn't exist as a package, with no ADR either committing to build it or formally deferring it. |
| 12 | No integration/E2E browser test suite for `apps/web` (Playwright named in `11-git-and-cicd-strategy.md`'s stage 6, never built) | Testing Strategy / CI-CD | Medium | Sprint 1 | No real UI exists yet to test beyond the status page — correctly deferred, but should land with the first real UI work, not as an afterthought once there's more to break. |
| 13 | Turborepo remote caching (Vercel) was never configured — `11-git-and-cicd-strategy.md` calls for it, `ci.yml` runs without it | Developer Experience / CI-CD | Low | Sprint 5 | Needs a Vercel account/token this environment doesn't have. Every CI run today does a full rebuild; acceptable at current monorepo size (30 packages, sub-10s local build), will matter more as the package count grows. |
| 14 | No `Documentation-as-code` fitness check covers `tools/*` packages (SAF-19's README-required check scopes to `packages/`/`plugins/`/`apps/` only) | Documentation / CI-CD | Low | Sprint 10 | Deliberate scope match to `12-risks-and-technical-debt.md`'s exact wording at the time SAF-19 was built. `tools/sprint0-demo` has a real README anyway; the check just doesn't enforce it mechanically. |

## What was explicitly *not* treated as debt

Everything in the Sprint 1/2 carry-forward list (SAF-24–37) — each has a named ADR, a stated trigger condition, and was never in Sprint 0's scope to begin with. Listing them here as "debt" would conflate deliberate sequencing with unplanned shortcuts, which is exactly the distinction [TECHNICAL_DEBT_POLICY.md](../../../TECHNICAL_DEBT_POLICY.md) draws.

## Corrected during this exit gate (not left for Sprint 1)

- `CONTRIBUTING.md`'s drifted environment-setup instructions (wrong compose file, wrong service list) — fixed.
- Missing `CODEOWNERS` and PR template (SAF-18) — added.
- SAF-23's bookkeeping gap — closed with an honest retroactive narrative, no new code needed.

No item above rose to **Critical**. Item 9 (CI never executed on a real runner) is the highest-severity open item and is explicitly gated on the user authorizing a push — not a code or architecture defect.
