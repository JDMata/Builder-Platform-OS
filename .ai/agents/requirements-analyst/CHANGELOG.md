# Changelog: requirements-analyst

## v1 (draft)
Initial definition, authored as the worked example proving the `.ai/` agent template end to end (see [ADR-0020](../../../docs/adr/0020-ai-workspace-for-agent-definitions.md)). Not yet backed by a real prompt evaluation or production usage.

**2026-07-17 (still v1, still draft):** first real invocation, by Sprint 1's VS-1 (`context-requirements`'s `structureBusinessRequirement` application service, resolved through `CapabilityResolverPort`'s first real adapter). The prompt's output-format instructions were completed (a concrete JSON shape, previously a placeholder reference) — no change to this agent's Purpose, Responsibilities, or Escalation rules.
